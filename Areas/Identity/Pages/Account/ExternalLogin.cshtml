@page "/su-dung-dich-vu-ngoai/"
@model ExternalLoginModel
@{
    ViewData["Title"] = "Tạo hoặc liên kết với dịch vụ ngoài";
}

<h1>@ViewData["Title"]</h1>
<h4 id="external-login-title">Liên kết với tài khoản @Model.ProviderDisplayName.</h4>
<hr />

<p id="external-login-description" class="text-info">
    Bạn đã xác thực thành công từ <strong>@Model.ProviderDisplayName</strong>.
    Hãy nhập địa chỉ Email để tạo hoặc liên kết
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Đăng ký</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
